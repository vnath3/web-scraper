Static HTML demo pages used as sales collateral to show prospects our
build quality. Each folder holds a standalone niche demo (HTML/CSS/JS,
mock data, no backend). Not connected to the scraper or CRM — capability
showcase only, not tied to sources.yaml niches. More will be added over time.
